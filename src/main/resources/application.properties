# MySQL connection properties
#spring.datasource.driver-class-name=
spring.datasource.username=root
spring.datasource.password=Alpina87!.
spring.datasource.url=jdbc:mysql://localhost:3306/bike_rental?useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Poland&allowMultiQueries=true 
#spring.datasource.initialization-mode=always

# inicjacja data data.sql.
# spring.datasource.initialization-mode=always + create -drop +id identity

# Log JPA queries
# Comment this in production
spring.jpa.show-sql=true

# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purpose - comment this in production
spring.jpa.hibernate.ddl-auto=create

# Hibernate SQL dialect     !!!!above create-drop
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect